# Justificación

Es prácticamente obligatorio en este stack porque R2 (almacenamiento) requiere una cuenta de Cloudflare de todas formas (ver `../04-almacenamiento-r2/`); consolidar DNS + CDN + WAF en el mismo proveedor simplifica la operación y es gratuito en su plan base.

